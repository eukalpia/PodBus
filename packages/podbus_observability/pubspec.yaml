name: podbus_observability
description: Structured logging, Prometheus metrics, and trace propagation for PodBus.
version: 0.1.0-alpha.1
repository: https://github.com/eukalpia/PodBus
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  podbus_core:
    path: ../podbus_core

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
