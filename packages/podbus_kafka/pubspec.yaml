name: podbus_kafka
description: Experimental Kafka event-log adapter for PodBus.
version: 0.1.0-dev
repository: https://github.com/podbus/podbus
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  kafka_dart: ^1.0.2-alpha
  meta: ^1.17.0
  podbus_core:
    path: ../podbus_core

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
