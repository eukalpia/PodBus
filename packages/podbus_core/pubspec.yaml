name: podbus_core
description: Transport-agnostic messaging and durable job queue abstractions for Dart backends.
version: 0.1.0-dev
repository: https://github.com/podbus/podbus
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  collection: ^1.19.1
  meta: ^1.17.0
  uuid: ^4.5.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
